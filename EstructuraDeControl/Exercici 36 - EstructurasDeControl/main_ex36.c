/* 
 * File:   main.c
 * Author: pdavila
 *
 * Created on 18 de diciembre de 2013, 10:11
 */

#include <stdio.h>
#include <stdlib.h>

/*
 * Exerici 36 - EstructurasDeControl
 * 
 * Un altre algorisme d'Euclides per trobar el mcd és el següent: donats els enters a i b (a>b), es
 * divideix a per b, obtenint el quocient q1 i el residu r1. Si r1!=0, es divideix b per r1, obtenint
 * el quocient q2 i el residu r2. Si r2!=0, es divideix r1 per r2, per obtenir q3 i r3, i així
 * successivament. Es continua el procés fins que s'obtingui un residu 0. El residu anterior és
 * aleshores el mcd dels nombres a i b. Escriviu un programa que calculi el mcd de dos nombres.
 * 
 */
int main(int argc, char** argv) {

    return (EXIT_SUCCESS);
}

