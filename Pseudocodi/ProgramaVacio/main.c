/* 
 * File:   main.c
 * Author: pdavila
 *
 * Created on 8 de noviembre de 2013, 8:56
 */

#include <stdio.h>              // Librería donde se encuntra la instrucción "printf" y "scanf"

/*
 * Primer programa vacio - Prueba
 */

int main() {
    
    // Este programa no hace nada
    
    /* Declaración de variables enteras
     * int a,b:                 // Dos variables
     * short mes;               // Variables enteras cortas
     * long tiempoTierra;       // Variables enteras largas
     */
    
    /* Declaración de variables reales
     * float f;                 // Una variable real
     * double g;                // 
     */
    
    /* Declaración cadenas de caracter --> són como un vector de caracteres
     * char c;                  // Variable un solo caracter
     * char cadena[25];         // Cadena de caracter
     */
    
    /* Declación de booleanos (verdadero o falso)
     * int fin;                 // 
     */
    
    /* Declaración de arrays enteros
     * int vectorentero[5];     // Vector entero de 5 posiciones
     */
    
    /* Leer y escribir 
     * printf ("Hola mundo");   // Imprimir por pantalla (escribir)
     * scanf ("%d", &a);        // Lee una variable introducida por teclado
     * 
     *                          %d --> Para variables enteras
     *                          %f --> Para variables reales
     *                          %c --> Para cadena de caracteres
     * Ejemplo:
     * 
     * int a;                                           // Declaro variable a
     * 
     * printf ("Introduce un entero: ");                // Pedir introducir un entero
     * scanf ("%d", &a);                                // Leer variable a
     * 
     * print ("El cuadro de %d vale %d", a, a*a);       // Producto de un entero 
     * /
    
    

}

