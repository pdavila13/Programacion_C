/* 
 * File:   main.c
 * Author: pdavila
 *
 * Created on 14 de febrero de 2014, 8:13
 */

#include <stdio.h>
#include <stdlib.h>
#define DIM 2

/*
Escriviu un programa que ompli un vector de 2 dimensions en valors aleatoris modul 10, i si
son divisibles, guardar el valor dels indexos, i una vegada s'hagi omplert del tot, mostrar els
valors dels indexos per pantalla de forma tabulada i ordenada.
*/

int divisible(int num);

int main(int argc, char** argv) {
    
    printf("Numeros divisibles %d", divisible);

    return (EXIT_SUCCESS);
}

int divisible(int num) {
    
    int vector[DIM];
    int num;
    
    
    
}

